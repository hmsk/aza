{
  "name": "root",
  "private": true,
  "scripts": {
    "build:meta": "npm --prefix packages/meta run build",
    "dev:meta": "npm --prefix packages/meta run dev",
    "dev:demo": "npm --prefix packages/demo run dev"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.0.2"
  }
}
